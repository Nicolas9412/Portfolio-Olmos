import { Monoton, Lato } from "@next/font/google";

export const monoton = Monoton({
  weight: ["400"],
  subsets: ["latin"],
});

export const lato = Lato({
  weight: ["100", "400", "700"],
  subsets: ["latin"],
});
